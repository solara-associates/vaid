{
  "_comment": "Evidence-backed prose claims about the VAID OPEN STANDARD that are NOT reducible to a single capability status (those live in capabilities.json). Each claim carries its evidence, the date it was last verified, and — when a date is set — the verifier and the verified_artifact backing it. FRESHNESS is enforced automatically by scripts/verify-claims.mjs in CI: a null date, a date with no verifier/verified_artifact, or a date older than the budget (default WARN 60d / FAIL 90d, per-claim max_age_days override) fails the build. That check proves freshness, NOT truth — truth (whether the claim actually holds) remains a human review item at release, backed by each claim's evidence. SCOPE: this register attests to the open standard only; SYNTHERA commercial/product-posture claims (federation, policy enforcement, audit) live in the synthera repo's own claims register, so product-posture drift reds synthera CI, not the standard's. See docs/adr/0002-capabilities-manifest.md.",
  "version": 1,
  "claims": [
    {
      "id": "framework-governance-demonstrated",
      "claim": "Governance — the same VAID identity and attenuated delegation — has been demonstrated across ADK, LangChain and OpenAI adapters: one governance layer, not one per framework. (Live policy enforcement and audit posture are a SYNTHERA product concern, tracked in the synthera repo's claims register, not here.)",
      "evidence": [
        "forge-agents/harness/phase1e_adk_conformance.py (live: governance survives the framework swap to ADK)",
        "forge-agents/harness/phase1e_openai_conformance.py (live: same, framework=openai)",
        "vaid python/vaid-langchain (published to PyPI, conformance-tested)"
      ],
      "date_last_verified": "2026-07-28",
      "verifier": "phase1e-conformance Cloud Run job, execution phase1e-conformance-q7pfr (synthera-substrate-prod / europe-west1); image tag :6267345, forge-agents SHA 62673455d33f6834a0063ca879f67994db9a8126",
      "verified_artifact": "forge-agents/harness/artifacts/phase1e-conformance-2026-07-28.md",
      "verified_note": "Verified by a dated PASSING run of the phase1e LIVE conformance harnesses: ADK 7/7, OpenAI 9/9 across both delegation surfaces. Wire evidence shows real substrate mints, distinct attenuated child VAIDs, and over-envelope children denied at the substrate mint wall (least-privilege attenuation, ADR-0027 Phase 2). CORRECTION (2026-07-29): the claim previously read 'the same identity, policy and audit' — but the run proves identity + attenuated delegation, NOT policy evaluation or audit writes. Scoped to what the artifact backs; live policy/audit posture is a product claim in the synthera register. LangChain rests on published+conformance-tested vaid-langchain, not this live run."
    }
  ]
}
